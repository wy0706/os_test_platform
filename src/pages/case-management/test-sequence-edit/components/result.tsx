import React, { useEffect, useState } from "react";

const ResultPage: React.FC = () => {
  const [data, setData] = useState<any>(null);

  useEffect(() => {}, []);

  return <div className="result-page">测试结束</div>;
};

export default ResultPage;
