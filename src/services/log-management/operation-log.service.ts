import { request } from "@umijs/max";

const baseUrl = "/optLog";

export async function getList(params: any) {
  const result: any = await request<{}>(`${baseUrl}/getList`, {
    method: "GET",
    params: params,
  });
  return result;
}
